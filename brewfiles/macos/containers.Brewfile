# Optional Docker-compatible local container feature.
# The bootstrap installs tooling only; it does not start Colima or change the
# active Docker context.

brew "colima"
brew "docker"
brew "docker-compose"
